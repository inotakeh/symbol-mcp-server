# Supply-chain hardening. Install scripts are disabled: no package may run code at install time.
# If a dependency genuinely needs a postinstall step, a human decides and documents the exception here.
ignore-scripts=true
save-exact=true
engine-strict=true
fund=false
audit=true
